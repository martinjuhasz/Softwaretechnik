package de.teamrocket.relaxo.util.logger;

/**
 * Typen von Logger, die Instanziiert werden können.
 * Dient allein zur indentifizierung.
 */
public enum RelaxoLoggerType {
    REST,
    MESSAGING,
    JOB,
    WORKFLOW,
    DATABASE,
    CONTROLLER,
    EVENT,
    CONFIG,
    OTHER,
    TEST
}
